{
  "name": "@vault-mcp/core",
  "version": "0.0.1",
  "description": "Shared types and utilities for vault-mcp packages",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "tsc && node --import tsx --test 'tests/*.test.mjs' 'tests/*.test.ts'"
  },
  "dependencies": {
    "chokidar": "^5.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
